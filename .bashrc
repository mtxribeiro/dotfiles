[[ $- != *i* ]] && return

alias grep='grep --color=auto'
alias ls='ls --color=auto'

alias la='ls -a'
alias ll='ls -l'
alias lla='ls -la'

alias ..='cd ..'

alias cp='cp -r'
alias rm='rm -r'

alias cl='clear'

alias sp='sudo pacman'

PS1='\w '
