// PULSEAUDIO

{
  "pulseaudio": {
    "tooltip": false,
    "format": "{icon}",
    "format-muted": "",
    "format-icons": {
      "default": ["", "", ""]
    },
    "on-click": "hyprctl dispatch exec '[float; size 750 500; center] alacritty -e wiremix'",
    "on-click-right": "wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle"
  }
}
